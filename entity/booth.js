'use strict';

class Booth {

    /**
     * 
     * @param {string} url twitter URL
     * @param {string} name ユーザ名
     * @param {string} day 日付
     * @param {string} place 場所(西館、東館など)
     * @param {string} hole ホール情報
     * @param {string} position 場所
     */
    constructor (
        url,
        name,
        day,
        place,
        hole,
        position
    ) {}
}

module.exports = Booth;
